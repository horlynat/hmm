<aside
	class="w-64 h-screen bg-(--bg-default) text-(--brand-dark) flex flex-col">

	<!-- Logo / Titre -->
	<div class="p-6 border-b border-gray-200">
		<h1 class="text-xl font-heading font-bold">Soft UI Dashboard</h1>
		<p class="text-sm text-gray-500">Portfolio Admin</p>
	</div>

	<!-- Navigation -->
	<nav
		class="flex-1 p-4 space-y-8 overflow-y-auto">

		<!-- Principal -->
		<div>
			<p class="text-[10px] uppercase text-gray-400 font-bold mb-2 ml-4">Principal</p>
			<a href="{{ path('dashboard_index') }}" class="flex items-center px-4 py-2 rounded-lg hover:bg-gray-50 transition">
				<i class="ti ti-home mr-3"></i>
				Dashboard
			</a>
		</div>

		<!-- Gestion du contenu -->
		<div>
			<p class="text-[10px] uppercase text-gray-400 font-bold mb-2 ml-4">Contenu</p>
			<a href="{{ path('project_index') }}" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-briefcase mr-3"></i>
				Projets</a>
			<a href="{{ path('skill_index') }}" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-code mr-3"></i>
				Compétences</a>
			<a href="{{ path('article_index') }}" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-notebook mr-3"></i>
				Articles</a>
			<a href="{{ path('course_index') }}" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-school mr-3"></i>
				Formations</a>
			<a href="{{ path('experience_index') }}" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-history mr-3"></i>
				Expériences</a>
		</div>

		<!-- Utilisateurs -->
		<div>
			<p class="text-[10px] uppercase text-gray-400 font-bold mb-2 ml-4">Utilisateurs</p>
			<a href="{{ path('user_index') }}" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-users mr-3"></i>
				Clients</a>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-shield mr-3"></i>
				Admin</a>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-mail mr-3"></i>
				Contacts</a>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-inbox mr-3"></i>
				Requests</a>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-chat mr-3"></i>
				Témoignages</a>
		</div>

		<!-- Sécurité -->
		<div>
			<p class="text-[10px] uppercase text-gray-400 font-bold mb-2 ml-4">Sécurité</p>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-lock mr-3"></i>
				Logs</a>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-shield-check mr-3"></i>
				Sessions</a>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-key mr-3"></i>
				Rôles & Permissions</a>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-eye mr-3"></i>
				Audit & Monitoring</a>
		</div>

		<!-- Paramètres -->
		<div>
			<p class="text-[10px] uppercase text-gray-400 font-bold mb-2 ml-4">Paramètres</p>
			<a href="~" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-settings mr-3"></i>
				Configuration</a>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-bell mr-3"></i>
				Notifications</a>
			<a href="#" class="flex items-center px-4 py-2 hover:bg-gray-50">
				<i class="ti ti-plug mr-3"></i>
				Intégrations</a>
		</div>
	</nav>

	<!-- Footer -->
	<div class="p-4 border-t border-gray-200 bg-(--bg-default)">
		<p class="text-xs text-gray-400 mb-2 text-center">Soft UI Dashboard v1.0</p>
	</div>
</aside>

